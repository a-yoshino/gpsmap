<%= form_for(@gp) do |f| %>
  <% if @gp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gp.errors.count, "error") %> prohibited this gp from being saved:</h2>

      <ul>
      <% @gp.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :lat %><br>
    <%= f.text_field :lat %>
  </div>
  <div class="field">
    <%= f.label :lon %><br>
    <%= f.text_field :lon %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
